# hemang.dev

Personal site of Hemang Kumar. SvelteKit static build, deployed to GitHub Pages via Actions.

```sh
npm install
npm run dev      # local dev
npm run build    # static output in build/
```

Pushing to `master` deploys via `.github/workflows/deploy.yml`. GitHub Pages must be set to "GitHub Actions" as the source in repo settings.
